linters:
  enable:
    - gofmt
    - revive
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  timeout: 5m
  tests: true
